[pytest]
testpaths = tests
pythonpath = .
asyncio_mode = auto
norecursedirs = .venv .git .agents
markers =
    integration: Tests that require real PostgreSQL, Redis (NullPool), and full ASGI stack.
    unit: Tests that run with mocked dependencies, no external services needed.
